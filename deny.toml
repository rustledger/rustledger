# cargo-deny configuration
# https://embarkstudios.github.io/cargo-deny/

[graph]
targets = [
  "x86_64-unknown-linux-gnu",
  "aarch64-unknown-linux-gnu",
  "x86_64-apple-darwin",
  "aarch64-apple-darwin",
  "wasm32-unknown-unknown",
]

[advisories]
version = 2
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
yanked = "warn"

# rand 0.8.x (via cap-rand/wasmtime-wasi) uses thread_rng which is
# technically affected by RUSTSEC-2026-0097, but the unsound code path
# requires a custom logger calling rand::thread_rng(), which rustledger
# never does. Remove this ignore once wasmtime updates cap-rand to rand 0.9+.
[[advisories.ignore]]
id = "RUSTSEC-2026-0097"
reason = "rand 0.8.x thread_rng unsoundness requires a custom logger calling rand::thread_rng(), which rustledger never does. Pinned by cap-rand via wasmtime-wasi 43.x"

[licenses]
version = 2
allow = [
  "GPL-3.0-only",
  "GPL-3.0-or-later",
  "MIT",
  "Apache-2.0",
  "Apache-2.0 WITH LLVM-exception",
  "BSD-2-Clause",
  "BSD-3-Clause",
  "ISC",
  "Zlib",
  "0BSD",
  "Unicode-3.0",
  "MPL-2.0",
  "CDLA-Permissive-2.0",  # webpki-roots
  "LGPL-3.0",
  "CC0-1.0",              # ppmd-rust (zip dependency)
  "bzip2-1.0.6",          # libbz2-rs-sys (zip dependency)
]
confidence-threshold = 0.8

[[licenses.clarify]]
name = "ring"
expression = "MIT AND ISC AND OpenSSL"
license-files = [{ path = "LICENSE", hash = 0xbd0eed23 }]

[bans]
multiple-versions = "warn"
wildcards = "allow"
highlight = "all"

# Deny specific crates
deny = []

# Skip specific crate versions
skip = []

# Skip entire dependency trees
skip-tree = []

[sources]
unknown-registry = "deny"
unknown-git = "warn"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
