[package]
name = "duplicate-detect-plugin"
version = "0.1.0"
edition = "2021"
description = "Plugin that detects potentially duplicate transactions"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
rmp-serde = "1.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = "s"
lto = true
