\* TLC Configuration for BookingMethods.tla

CONSTANTS
    Currency = "AAPL"
    CostCurrency = "USD"
    MaxLots = 4
    MaxUnits = 20

INIT Init

NEXT Next

INVARIANTS
    Invariant
    TypeOK
    CostBasisTracked

\* Note: FIFOProperty and LIFOProperty are documented invariants
\* but currently trivially TRUE - see ROADMAP.md for strengthening plan
